// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['hips.hearstapps.com','anapincolini.com.br'],
    },
  };
  
  export default nextConfig;
  